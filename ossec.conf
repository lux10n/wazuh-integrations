  <!-- To add to ossec.conf -->

  <!-- Cloudflare logs -->
  <localfile>
    <location>/path/to/cloudflare.log</location>
    <log_format>json</log_format>
    <label key="@source">Cloudflare</label>
  </localfile>

  <!-- Github logs -->
  <localfile>
    <location>/path/to/github.log</location>
    <log_format>json</log_format>
    <label key="@source">Github</label>
  </localfile>